# Uncertainty Diagnostics

The default uncertainty diagnostics preserve numerical `NaN` values for
legitimate undefined edge cases, such as uncertainty-error correlation when all
predictive standard deviations are constant. Use the JSON-safe helpers for API
payloads, dashboards, and report manifests that require strict JSON.

::: matgpr.uncertainty
