# Validation

Reusable model-validation workflows for train/test splits, cross-validation,
and configurable learning curves. Use sparse multitask helpers when `NaN`
target entries mean a property was not observed for a sample.

::: matgpr.validation
