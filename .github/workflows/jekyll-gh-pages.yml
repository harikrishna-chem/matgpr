# Disabled sample workflow. MkDocs deployment is handled by docs.yml.
name: Disabled Jekyll Pages sample

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement
        run: echo "This sample workflow is disabled. MkDocs deploys through .github/workflows/docs.yml."
